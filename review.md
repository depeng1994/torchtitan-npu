# PR Review: [feat] Extend DeepSeek-V4 LoRA training coverage

- PR: #22 (GitCode mirror !789)
- Review branch: `pr_789`
- Reviewed commit: `a9e35cae84a77424e17899cccb24e33e95591aa2`
- Base: `master@518357ea4bb3651a7477806f2042df04fa05ff53`
- PR size: 15 files, +1052/-107
- Background: extend DeepSeek-V4 LoRA training with Muon grouping/compute layouts, TorchAO-NPU quantized-base + floating LoRA adapters, PEFT export/offline merge, A5 launch coverage, and LoRA-specific selective-AC handling.
- Review policy: treat every new abstraction/helper/state/path as unnecessary until reuse or independent semantics justify it; prefer upstream TorchTitan hooks and one source of truth.

## Part 1/3 — `torchtitan_npu/models/deepseek_v4/` source review

Scope in this part:
`lora.py`, `config_registry.py`, `parallelize.py`, `state_dict_adapter.py`.

Upstream/reference baseline checked for this part:
TorchTitan `v0.3.0` (the version pinned by this repository), especially upstream LoRA converter ordering, `SelectiveAC.get_save_ops()`, `derive()`, mixed optimizer grouping, and the repository's TorchAO-NPU `ParamSwapConfig`/parameter-recursion implementation.

### Findings

| ID | Severity | Code location | Problem | Impact | Suggested change |
| --- | --- | --- | --- | --- | --- |
| P1-R1 | **P1** | `lora.py: unpack_expert_factors(), _expert_targets(), build_lora_merge_plan(), merge_lora_weight()` | The offline merge plan does not validate the adapter's semantic rank/expert cardinality against the base model. For ordinary dense adapters, the `rank` argument is not checked at all: an A/B pair whose inner rank is internally consistent but differs from `adapter_config.r` still reaches `merge_lora_weight()`, where the matrix shape can be valid and the merge succeeds with the wrong `alpha / r` scaling. For routed experts, `experts = a.shape[0] // rank` is inferred only from the packed adapter. If the supplied rank is wrong but divides `a.shape[0]`, the reshape still succeeds; for split per-expert base checkpoints, the code then emits targets only for `range(experts)`. A base with more experts therefore accepts a shorter inferred prefix and silently leaves the remaining experts unmerged. In the worst case, a wrong rank also re-partitions packed columns/rows across experts while keeping every resulting 2-D delta shape valid. | A malformed/mismatched adapter can produce a checkpoint that loads successfully but contains numerically wrong or only partially merged experts. This is substantially worse than a fail-fast shape error. | Validate every A/B pair against `rank` before building targets: dense LoRA must require `A.shape[0] == B.shape[1] == rank`. For expert adapters, require `A.shape[0] == B.shape[1] == expected_num_experts * rank`. Derive `expected_num_experts` from the split expert keys in `base_weight_map` when available; for fused expert tensors, pass the model expert count into the plan (or validate it when the fused base tensor is loaded) rather than treating the adapter-implied count as authoritative. Also require the discovered per-expert ID set to exactly match the expected contiguous expert set, not merely that every generated prefix target exists. |
| P1-R2 | **P2** | `lora.py: _make_lora_adapter_config_cls().Config.__post_init__()/base_filter` | The TorchAO-NPU composition currently replaces the original `ParamSwapConfig.params_filter_fn` semantics with a DeepSeek-V4 allowlist: only `weight/w1_EFD/w2_EDF/w3_EFD` plus `ndim >= 2` may pass. The needed semantic delta is much smaller: preserve the quantization policy for the base module and exclude LoRA adapter parameters. Because `ParamSwapConfig` recursively supplies parameter FQNs, this can be expressed as a negative adapter filter composed with the original filter. The current allowlist means any future base parameter selected by the original policy is silently not quantized only when LoRA is enabled. | Quantized non-LoRA and quantized-LoRA versions of the same module can drift in base precision semantics after an upstream/repository module change. The hardcoded list also creates a second place that must know every base parameter spelling. | Keep the existing `copy(policy)` (it correctly avoids mutating the shared policy object), but make the wrapper preserve the original predicate and only reject adapter FQNs, e.g. base selection = `original_filter(param, fqn)` AND “not a LoRA adapter parameter”. Do not add an independent `ndim` restriction unless that restriction already belongs to the original quantization policy. This reduces the LoRA/quantization coupling to its actual requirement: adapters stay floating. |
| P1-R3 | **P2** | `lora.py: LoRASelectiveAC`; `parallelize.py: parallelize_deepseek_v4()` | The AC workaround does not compose with the upstream extension mechanism it is built on. TorchTitan v0.3.0 explicitly exposes `SelectiveAC.get_save_ops()` for subclasses, but the activation policy is replaced only when `type(ac_config) is SelectiveAC.Config`. Any legitimate derived SelectiveAC config silently skips the LoRA bmm rule. At the same time, `LoRASelectiveAC.get_save_ops()` removes **all** `aten.bmm` from the save set for every LoRA block, while its comment attributes the need to `Indexer.select`'s in-place ReLU; that Indexer behavior itself is not LoRA-specific. The gating condition and the stated root cause therefore do not line up. | A future/upstream custom SelectiveAC subclass can reintroduce the mutation/recompute hazard without an error, while the default LoRA path pays a broader-than-explained bmm recomputation cost. This is exactly the sort of fragile subclass interaction likely to break on upstream upgrades. | First make the root cause/scope explicit. If the correctness issue is the Indexer in-place mutation, prefer fixing/isolating that mutation or a DSV4-level SAC policy instead of creating a LoRA-only policy. If the rule is genuinely LoRA-specific, make policy composition explicit; at minimum fail fast for an unsupported derived `SelectiveAC.Config` rather than silently bypassing the rule. Do **not** simply broaden the check to `isinstance` and derive into `LoRASelectiveAC.Config`, because that would silently drop fields/semantics owned by the custom subclass. |
| P1-R4 | **P2** | `config_registry.py: _dsv4_muon_profile()/muon_pattern` | LoRA compute layouts are mostly derived from the existing base `shardings` plus `DEEPSEEK_V4_LORA_TARGETS`, but Muon membership is separately re-spelled as a long regex list. Those two decisions can now drift independently. In particular, adding a new LoRA target to `MODULE_PATHS/DEEPSEEK_V4_LORA_TARGETS` can automatically create its LoRA config/export mapping and even its derived compute layout, yet the optimizer regex can omit it; the catch-all AdamW group then accepts it silently, so there is no configuration error. | A later target addition can unexpectedly change optimizer choice from the intended Muon behavior to AdamW with no failure, despite a valid compute layout being present. This duplicates model topology knowledge in exactly the area this PR is trying to centralize. | Establish one source of truth for “LoRA parameters admitted to Muon” and use it for both regex generation and compute-layout generation. Keep only the genuine exception explicit: `w13_lora_b[E,F,2,R]` is intentionally excluded and falls through to AdamW. Add an invariant/assertion that every LoRA parameter matched by the Muon group has a compute layout and every intended Muon LoRA layout is reachable by the Muon pattern. |

### Verified items / no additional issue in Part 1

| Area | Result |
| --- | --- |
| Quantized base + LoRA conversion order | **Checked.** The repository first applies the model LoRA converter, then `TrainerEx` applies TorchAO-NPU. `NpuQuantizeConverter` dynamically subclasses the already-LoRA owner and `derive()` constructs the quantized Config; the inherited LoRA `__post_init__` therefore sees the newly injected `_torchao_npu_config`. This path is functional. |
| Shared quantization policy isolation | **Checked.** Copying `ParamSwapConfig` before replacing `params_filter_fn` is necessary because the NPU quantization converter passes the same base policy object to multiple converted nodes. The copy keeps non-LoRA nodes from being modified. Keep this part. |
| Non-LoRA behavior | **Checked.** The new quantization filter wrapping lives only on dynamically generated LoRA Config classes; the AC replacement is gated on `model.lora_config`; AdamW remains the default optimizer schema. The four-file diff does not directly change ordinary non-LoRA model execution today. |
| `LoRALinear.forward(...).clone()` | **Checked; keep it.** This implementation adds adapter deltas in-place (including chunked additions) after the base operator returns. Cloning gives the LoRA accumulation its own storage instead of mutating an operator result/view that SAC/autograd/backend code may have saved. The clone is a justified consequence of the in-place/chunked implementation, not dead copying. |
| Muon layouts | **Checked.** Dense LoRA A uses `Owned`; LoRA B reuses the corresponding base layout, which is important for the per-head/per-group `wq_b/wo_a` `BlockShard` cases. Routed `w13_lora_a/w2_lora_a/w2_lora_b` reuse expert-axis sharding. `w13_lora_b[E,F,2,R]` is deliberately absent from the Muon regex/layout and therefore falls through to AdamW, matching the stated interleaved-storage constraint. |
| Expert pack/unpack algebra | **Checked for valid metadata.** A packs as `[E,R,D] -> [E*R,D]`; ordinary B packs as `[E,O,R] -> [O,R*E]`; `w13_lora_b` first concatenates gate/up to `[E,2F,R]` and then uses the same PEFT B layout. `unpack_expert_factors()` reverses the packed expert/rank axes into the form required by the merge planner. The remaining problem is validation/cardinality, captured in P1-R1. |
| `MODULE_PATHS` / state-dict mapping | **Checked.** The generated `OFFICIAL_WEIGHT_MAP` preserves the explicit head/attention/router/shared-expert/MTP projection mappings removed from `state_dict_adapter.py`. Compressor entries are intentionally excluded there because compressor checkpoint mappings remain layer/compress-ratio dependent and are still added dynamically. No current non-LoRA checkpoint key regression was found in this refactor. |
| PEFT export expert packing reuse | **Checked.** Replacing the duplicated packing code in `state_dict_adapter.to_peft()` with `pack_expert_factor()` preserves the previous layout while giving the offline merge path the same packing convention. |
| Upstream coupling | **Checked.** No new code is placed in `torchtitan_npu/patches`; the selective-AC customization uses the upstream `get_save_ops()` hook rather than monkey-patching TorchTitan. P1-R3 is about composition/scope of that hook, not the choice to use the hook itself. |

Part 1 conclusion: **changes requested**. P1-R1 should be fixed before merge because the current offline merge can silently produce a semantically invalid checkpoint. P1-R2/P1-R3/P1-R4 should be addressed to keep quantization, AC, and Muon behavior composable and resistant to upstream/model evolution.


## Part 2/3 — offline merge tool and LoRA training/merge tests

Scope in this part:
\`scripts/lora/merge_adapter.py\`,
\`tests/unit_tests/models/deepseek_v4/test_lora_merge.py\`,
\`tests/unit_tests/models/deepseek_v4/test_lora_training.py\`.

Test-review baseline:
repository \`.agents/skills/developer-tests-review\` review workflow, root \`tests/unit_tests\` entry, current \`tests/integration_tests\` runner/README, and TorchTitan \`v0.3.0\`.
The declared dev dependency is \`transformers==4.57.6\`.

**测试执行：未执行（仅静态审查）**。

### Findings

| ID | Severity | Code location | Problem | Impact | Suggested change |
| --- | --- | --- | --- | --- | --- |
| P2-R1 | **P1** | \`test_lora_merge.py: test_merge_plan_rejects_incomplete_or_unmatched_adapter(), test_merge_tensor_rejects_shape_mismatch()\` | The negative tests do not cover the exact silent-corruption cases from P1-R1. They reject orphaned A/B keys and final matrix-shape mismatch, but never pass a dense A/B pair whose inner rank is self-consistent yet disagrees with \`adapter_config.r\`, and never pass a packed routed-expert adapter whose inferred expert count is smaller than the base checkpoint's expert set. Both malformed inputs can currently keep all generated 2-D merge shapes valid. | P1-R1 can regress or remain unfixed while the merge suite stays green; the dangerous case is not “merge throws”, but “merge succeeds with wrong scaling or only a prefix of experts”. | Add two focused failure tests alongside the existing merge-plan tests: (1) dense A/B with e.g. physical rank 4 but config rank 2, where \`B @ A\` still matches the base weight shape, must raise on rank mismatch; (2) packed expert A/B for N experts against a base \`weight_map\` containing more experts must raise on expert-cardinality mismatch. The expected value must come from config/base metadata, not from \`unpack_expert_factors()\` itself. |
| P2-R2 | **P2** | \`merge_adapter.py: merge()\` quantized-base preflight; repository \`torchao_npu.serialization.save_hf_safetensors()\` | The “convert quantized checkpoints to floating point before merging” contract is only recognized when \`config.json\` contains \`quantization_config\`/selected \`expert_dtype\` values or when the **target logical tensor itself** has a non-F16/BF16/F32 safetensors dtype. This misses the repository's own TorchAO-NPU serialized MX form: \`save_hf_safetensors()\` flattens a quantized logical weight into keys such as \`..._weight_qdata\` and \`..._weight_scale\`, so the logical \`.weight\` key can be absent. In that case \`build_lora_merge_plan()\` fails first with a generic “no matching base weight” error, before the script can emit the intended backend-conversion guidance. | A supported producer from this repository can hit an opaque key-mismatch error instead of the documented actionable quantized-checkpoint error. The implementation and documentation describe a stronger contract than the CLI actually diagnoses. | Detect the repository's quantized safetensors form before building the merge plan. Prefer a lightweight metadata/key preflight (TorchAO safetensors metadata or the logical target's \`_weight_qdata/_weight_scale\` siblings) and raise one explicit message such as “TorchAO-NPU quantized checkpoint detected; export/decode it to FP16/BF16/FP32 with the corresponding quantization backend before running merge_adapter”. Add a test built with the repository's \`save_hf_safetensors()\` rather than only a raw float8 tensor. |
| P2-R3 | **P2** | \`merge_adapter.py: adapter = load_file(...)\`; \`build_lora_merge_plan()\` call | The base checkpoint is processed one target shard at a time, but the adapter is eagerly materialized in full via \`load_file\`. Routed-expert LoRA is exactly the case where adapter size scales with expert count and layer count; peak host memory is therefore at least the full adapter plus the largest rewritten base shard, with additional FP32 merge temporaries from \`merge_lora_weight()\`. | The “only rewrite shards containing merge targets” I/O optimization does not bound memory for the largest DeepSeek-V4 adapter configurations, so the offline tool can OOM on the scenario the expert packing support is intended to serve. | Keep the current per-base-shard rewrite, but lazily read adapter factors with \`safe_open\`. Build a lightweight target plan from adapter keys/shapes, then materialize only the A/B pair(s) needed for the shard/target being rewritten and release them before advancing. Avoid adding a general checkpoint abstraction; this can remain a small merge-tool-specific data path. |
| P2-R4 | **P2** | \`merge_adapter.py: os.makedirs(output_dir)\` followed by copy/rewrite loop | Validation is fail-before-create, but the write phase is not transactional. Once \`output_dir\` is created, any later \`copy2\`, tensor materialization, disk-full, or \`save_file\` failure leaves a partial directory. A rerun then fails immediately because the output is required not to exist. | A failed merge leaves an output path that looks like a model directory but is incomplete, and the documented rerun contract requires manual cleanup. | Write into a sibling temporary directory after all preflight checks, then atomically rename it to \`output_dir\` only after every copy and shard rewrite succeeds; remove the temporary directory on exception. Keep the user-facing requirement that the final output must not pre-exist. |
| P2-R5 | **P2** | \`test_lora_merge.py: transformers_adapter, test_current_export_merges_transformers_experts_and_matches_logits()\`; \`requirements-dev.txt\` | The strongest merge oracle is statically unreachable under the repository's declared dev dependency. The fixture calls \`pytest.importorskip("transformers.models.deepseek_v4")\`, while pinned Transformers \`v4.57.6\` contains \`deepseek_v2\`/\`deepseek_v3\` but no \`deepseek_v4\` module. Therefore the test that exercises Transformers fused expert storage, reloads the merged model, and compares logits is skipped in the declared UT environment. | Fused-expert merge/reload correctness is presented as tested but is not actually protected by the normal dependency set. | Prefer deleting the dependency on an unavailable Transformers model from this unit oracle: construct the minimal fused \`gate_up_proj/down_proj\` safetensors fixture directly and compare merged tensors/logits against an independent small PyTorch formula. Only raise the repository Transformers pin if the product itself needs that newer dependency; do not upgrade a large dependency solely to keep one test alive. |
| P2-R6 | **P1** | \`test_lora_training.py: ao fixture, test_quantization_preserves_adapter_weights_and_gradients(), test_registered_quantized_lora_accepts_compile_configuration()\`; \`tests/integration_tests/deepseek_v4.py\`; \`.ci/integration_test.sh\` | The headline “quantized base + floating LoRA” training path has no non-optional end-to-end NPU test. CPU tests route through \`pytest.importorskip("torchao_npu")\`; the repository-controlled root UT entry does not install the experimental \`torchao-npu\` package/\`torchao==0.17.0\`, so static review cannot count those cases as guaranteed execution. Even when present, the test replaces NPU matmul kernels with CPU functions. The default NPU \`dsv4_lora_ep2_fsdp2\` case now selects Muon but never enables \`--extension.quantization.enable-quantized-training\`. Finally, \`test_registered_quantized_lora_accepts_compile_configuration\` sets compile config and meta-builds a model but never invokes compilation, so it is not compile-path evidence. | The most NPU-specific new feature can fail in wrapper dispatch, FSDP/EP interaction, compile, or real backward while all default integration tests still pass. | Add one minimal registered NPU integration case using the real LoRA training config plus one supported TorchAO-NPU recipe (no need to duplicate all three recipes). It must actually enable quantized training, enter real forward/backward/optimizer step, and assert the base is quantized while LoRA parameters remain floating/trainable. If compile support is part of the claimed path, make that same case enable the actual compile backend rather than treating a meta build as compile coverage. Separately, ensure the CPU quantization tests are non-skippable in the repository's intended UT environment or move the dependency installation/coverage to an explicitly executed experiment test job. |
| P2-R7 | **P2** | \`tests/integration_tests/lora_config.py: _optimizer_snapshot()/train_step()\`; \`tests/integration_tests/deepseek_v4.py: dsv4_lora_resume_ep2_fsdp2\`; \`.ci/integration_test.sh\` | The resume assertion itself is strong: at the first resumed step it compares the live optimizer state against the step-2 snapshot before the next optimizer update with exact tensor equality, and the runner also compares resumed loss/grad_norm against uninterrupted steps. However, the case is only returned by the \`deepseek_v4_checkpoint\` suite, while the repository CI entry invokes only \`--test_suite models\`. The new LoRA resume case is therefore not selected by the normal integration job. The integration README also does not list this new case. | The code contains a valid checkpoint-resume test, but it does not protect PRs in the default CI path and the documented ST matrix is stale. | Keep the current snapshot oracle; do not weaken it to “checkpoint exists”. Make the LoRA resume case reachable from the normal repository integration selection (prefer the existing model-suite registration mechanism rather than a one-off CI command), and update \`tests/integration_tests/README.md\` to include the case and its exact optimizer-state + loss/grad_norm checks. |
| P2-R8 | **P2** | \`test_lora_training.py: test_muon_group_is_non_empty_and_every_member_has_a_compute_layout()\` | The test proves only one direction: every parameter that the regex already assigned to DistMuon has a compute layout. It does **not** prove the inverse invariant from P1-R4: every LoRA parameter that is intended to use Muon is actually selected by the regex. If a newly supported target receives a compute layout but silently falls through to catch-all AdamW, this test still passes. The few \`any(...)\` spot checks do not close that gap. | The test cannot detect the exact future drift described in P1-R4. | Derive the expected Muon LoRA FQN set from the actual converted model/declared target set, subtract only the documented \`w13_lora_b\` AdamW exception, and assert set equality with the actual DistMuon assignment. Then separately assert every member of that expected set has a compute layout. |
| P2-R9 | **P3** | \`test_lora_training.py: test_smoke_recipe_optimizer_selection(); quantize_first parameterizations\` | Several new cases exercise test-only or non-production construction rather than independent product semantics. The unit test imports \`tests.integration_tests.lora_config.deepseek_v4_lora_training\` and calls it a “smoke recipe”, coupling CPU UT to ST infrastructure. Both quantization tests also double their matrix with \`quantize_first=True\`, manually invoking private \`_make_lora_config()\`; the real repository path is model LoRA conversion first, then \`TrainerEx\` applies TorchAO-NPU quantization. No user-facing entry in this PR selects the reverse order. | Extra cases increase maintenance cost and can give false confidence about paths users never execute, while the real quantized training path still lacks ST coverage. | Delete the test-only integration-config dependency (the following Muon grouping test already checks the production optimizer schema more directly). Remove the reverse-order parameterization unless it is promoted to a documented supported API. Concentrate the remaining tests on the real registry → LoRA converter → \`TrainerEx\` quantization order. |

### CPU UT positive-function coverage

| Positive function | Production path / expected result | PR direct check | Status | Merge prerequisite |
| --- | --- | --- | --- | --- |
| Offline merge of current exported dense + split expert adapters | PEFT A/B → \`build_lora_merge_plan\` → \`merge_lora_weight\`; exact base tensors change by \`alpha/r * B@A\`, original base stays unchanged | \`test_current_export_merges_checkpoint_weights_and_reloads_state\` builds an adapter through the real state-dict adapter and compares every written target tensor against an independently updated expected tensor for official and renamed Transformers-style split keys | 已覆盖 | None beyond P1-R1 validation fixes |
| Fused Transformers expert merge and reload | Fused \`gate_up_proj/down_proj\` base → merge → reload → identical state/logits to independently merged model | \`test_current_export_merges_transformers_experts_and_matches_logits\` contains a strong oracle, but its fixture is skipped with the pinned Transformers version | 未覆盖 | Replace the unavailable model dependency with a stable synthetic fused-expert oracle, or intentionally update the product/dev dependency |
| Rank and expert-cardinality validation | Adapter metadata/base expert set must reject semantically mismatched packed factors even when matrix multiplication shapes still fit | No direct check; current negatives only cover orphan keys and final delta shape mismatch | 未覆盖 | Add the two P2-R1 cases |
| Output-path protection | Final output must not pre-exist and must not be inside base/adapter | \`test_merge_protects_inputs_and_existing_output\` covers base child, adapter child, and pre-existing output | 已覆盖 | None |
| Quantized-base rejection with actionable guidance | Any supported quantized checkpoint representation must be rejected before merge with explicit float-conversion guidance | \`test_quantized_input_requires_explicit_conversion\` covers config-declared quantization and a raw float8 logical target, but not the repository's TorchAO flattened safetensors form | 部分覆盖 | Add repository-native quantized safetensors fixture per P2-R2 |
| Shard/index/config preservation | Only target shards are rewritten; untouched files remain byte-identical; rewritten target dtype remains base dtype | \`test_merge_preserves_untargeted_shards_and_metadata\` checks untouched shard/index/config bytes. Current end-to-end tensors are float32; FP16/BF16 dtype preservation is not directly asserted | 部分覆盖 | Add a small BF16 (and optionally FP16) merge assertion that checks both value and dtype; no need to duplicate all layouts |
| Quantized base + floating LoRA CPU contract | Real config composition keeps base matrix parameters wrapped, adapters ordinary/trainable, and adapter gradients flow | \`test_quantization_preserves_adapter_weights_and_gradients\` reaches real wrappers but stubs NPU kernels; the whole block is optional through \`importorskip\` | 部分覆盖 | Make the intended UT dependency path explicit/non-skippable, then keep one representative dense and one grouped case rather than unsupported order permutations |
| Muon assignment + compute layout | Intended LoRA matrices use DistMuon/layout; interleaved \`w13_lora_b\` alone falls to AdamW | Current test checks actual DistMuon members have layouts and spot-checks several FQNs | 部分覆盖 | Add inverse set-equality invariant from P2-R8 |

### NPU ST trigger check

| Production change | Target training path | Existing integration testcase | Status | Required adjustment |
| --- | --- | --- | --- | --- |
| LoRA + Muon mixed optimizer | Real DeepSeek-V4 LoRA, EP2/FSDP2, Muon for admitted adapter matrices and AdamW fallback | \`dsv4_lora_ep2_fsdp2\` is in the default \`models\` suite and now passes \`--optimizer.name=Muon\`; its trainer checks frozen base, adapter update and final PEFT export | 已覆盖 | Keep this case; it is the correct real-NPU complement to CPU grouping tests |
| Quantized base + floating LoRA | TorchAO-NPU parameter wrappers + real DeepSeek-V4 LoRA forward/backward, optionally compile if claimed | No registered default integration case enables \`--extension.quantization.enable-quantized-training\` | 未覆盖 | Add one minimal registered case per P2-R6; do not create a separate runner |
| LoRA optimizer/checkpoint resume | Save training state at step 2, restore Muon + AdamW states, continue steps 3/4 exactly | \`dsv4_lora_resume_ep2_fsdp2\` contains the right snapshot and dynamic exact-resume checks, but only in the non-default \`deepseek_v4_checkpoint\` suite | 部分覆盖 | Make the case part of the repository-controlled CI selection and document it |
| Offline adapter merge | Pure CPU safetensors/key mapping/linear algebra; no NPU kernel or collective is involved | CPU UT is the correct evidence layer | 无需 ST | Fix the CPU coverage gaps above; do not add NPU capacity for this tool |

### Test-format / isolation notes

| Location | Type | Static fact | Minimal change |
| --- | --- | --- | --- |
| \`test_lora_training.py::test_smoke_recipe_optimizer_selection\` | 测试入口 | A CPU unit test imports the integration-only \`LoRATrainer\` config and names the check “smoke”, although it only materializes optimizer config | Remove it if redundant with the production optimizer-group test; otherwise use a production config factory and a behavior-specific name |
| \`test_lora_training.py::test_quantization_preserves_adapter_weights_and_gradients\` | 状态隔离 | The test calls \`parameter.normal_()\` and \`torch.randn()\` on the global CPU RNG without a local generator/\`fork_rng\`, while this file has no autouse RNG-restoration fixture | Wrap the random preparation in \`torch.random.fork_rng(devices=[])\` or use a local \`torch.Generator\` |
| \`test_lora_training.py: ao fixture\` | 状态隔离 | The fixture temporarily prepends the experiment package path, but imported \`torchao_npu.*\` modules remain in \`sys.modules\` after the path is restored. Another repository test already explicitly snapshots/restores this import cache for the same package | Reuse the existing import-cache isolation pattern or make the fixture module-scoped and explicitly own/restore the imported package state |

### Verified items / no additional issue in Part 2

| Area | Result |
| --- | --- |
| CLI required arguments | **Checked.** \`--base-model\`, \`--adapter\`, and \`--output\` are all required by argparse. No extra shell wrapper/config layer is introduced. |
| Output-path preflight | **Checked.** \`realpath/commonpath\` rejects output equal to or nested under either input; an already existing output raises before any write. P2-R4 concerns failures after the write phase starts, not the initial validation. |
| Shard rewrite policy | **Checked.** The script computes the set of shards owning merge targets, copies unrelated top-level files byte-for-byte, and rewrites only those target shards. Within a rewritten shard it reloads all tensors so unrelated tensors in that shard are retained. |
| Index/config preservation | **Checked.** The original index and model config are copied unchanged when they are not themselves target shard files. Because merge preserves tensor shape and dtype, index \`total_size\` does not need recomputation for the supported FP16/BF16/FP32 path. |
| Base dtype | **Checked in production code.** \`merge_lora_weight()\` computes in FP32 and casts back to the original base dtype; shard preflight restricts targets to FP16/BF16/FP32. The remaining gap is direct BF16/FP16 test coverage, recorded above. |
| Safetensors metadata | **Checked.** Rewritten shard header metadata is read with \`safe_open().metadata()\` and passed back to \`save_file\`; untouched shards are copied byte-for-byte. |
| Merge planner reuse | **Checked.** The script does not duplicate module-path/expert packing rules; it delegates target construction and tensor merge math to \`build_lora_merge_plan()\` / \`merge_lora_weight()\`. P2-R3 is about eager materialization, not duplicated semantics. |
| Resume oracle strength | **Checked.** The new NPU resume trainer snapshots actual flattened optimizer tensor state after the saved step and compares it exactly before the first resumed optimizer update. The runner additionally requires exact loss and grad-norm equality for resumed steps. The deficiency is CI selection, captured in P2-R7. |
| Unit-test device boundary | **Checked.** The merge tests are CPU/filesystem tests. Quantization tests intentionally stub only NPU kernel boundaries; they do not prove real NPU execution and are not counted as ST. |

Part 2 test-review conclusion: **补充测试后合入**. P2-R1 and P2-R6 are merge-blocking coverage gaps for two headline semantics (safe offline merge validation and quantized-base LoRA training). P2-R5/P2-R7 must also be fixed so the strongest merge/resume checks are actually selected by the declared test environment/CI rather than existing only as skippable or manual cases.

> Part 3/3 is intentionally not covered in this commit.
